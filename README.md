Whenever you start working, use "git pull origin main" first.
At the end of the day, or whenever a milestone is reached. Use these commands in order:
git add .
git commit -m "Put some message here"
git push origin main

Check GitHub to make sure the commit have went through

PathPlanner Library
Velocity PID based on positions
LimeLight localization